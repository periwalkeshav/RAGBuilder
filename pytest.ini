[pytest]
testpaths = tests
addopts = -q --strict-markers --color=yes
markers =
    integration: needs the running stack (set RUN_INTEGRATION=1)
    slow: loads the embedding model or calls the LLM
filterwarnings =
    ignore::DeprecationWarning
